# v1 was being developed at the same time as Cartero. One of the early snapshots
# of Cartero allowed to pass the body as a string instead of as a table.
# This is equivalent to using a raw body with the type set to octet-stream.

version = 1
url = "https://www.example.com/api/users"
method = "PUT"
body = "request contents"

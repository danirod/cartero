option(
  'profile',
  type: 'combo',
  choices: [
    'default',
    'development'
  ],
  value: 'default',
  description: 'The build profile for Cartero. One of "default" or "development".'
)

option(
  'decorations',
  type: 'combo',
  choices: [
    'csd',
    'no-csd',
  ],
  value: 'csd',
  description: 'Whether to use client side decorations. One of "csd" or "no-csd"'
)

# Package maintainers: this option is designed for the AppImage, Windows and macOS
# artifacts hosted at the project website. You might want to keep this feature
# disabled if the package manager or repository already manages updates on behalf
# of the user (true for Flathub, Snapcraft, AUR, and many others).
option(
  'app-updater',
  type: 'feature',
  value: 'disabled',
  description: 'Whether to enable in-app software update checker',
)

# When running the install target, will also create an AppImage.
option(
  'appimage',
  type: 'feature',
  value: 'disabled',
  description: 'Whether to create an AppImage when installing',
)

option('win32-bundle', type: 'feature', value: 'disabled', description: 'Whether to bundle all external dependencies when running the install target on Windows')
option('win32-installer', type: 'feature', value: 'disabled', description: 'Whether to compile a Windows installer when running the install target on Windows')
option('win32-sign-subject', type: 'string', value: '', description: 'When bundling for Windows, the subject name or a substring that will be passed to signtool if provided')
option('macos-app', type: 'feature', value: 'disabled', description: 'Whether to create an .app when running the install target on macOS')
option('macos-dmg', type: 'feature', value: 'disabled', description: 'Whether to create an .dmg when running the install target on macOS')
option('macos-codesign-identity', type: 'string', value: '-', description: 'When creating an .app, the identity to use to sign the app')
option('macos-notary-profile', type: 'string', value: '', description: 'When creating a .dmg, the keychain profile to use to notarize the app')